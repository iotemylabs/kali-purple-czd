# Batch 08, calamares-welcome: NEEDS 28 GB · UEFI · 8 GB RAM. Storage and root are hard
# requirements; RAM below 8 GB is a warning, not a stop.
---
showSupportUrl:         true
showKnownIssuesUrl:     false
showReleaseNotesUrl:    true
requirements:
    requiredStorage:    28
    requiredRam:        8.0
    check:
        - storage
        - ram
        - power
        - root
    required:
        - storage
        - root
