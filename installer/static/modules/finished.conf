# Batch 08, calamares-slideshow, 04: done is a state, not a dialog. RESTART NOW is the one
# filled-gold action; staying in the live session is the quiet alternative.
---
restartNowEnabled: true
restartNowChecked: true
restartNowCommand: "systemctl -i reboot"
